"""Smoke tests for src/kindex/vectors.py (auto-generated by pact adopt)."""

import pytest

def test_import_kindex_vectors():
    """Module imports without error."""
    import importlib
    mod = importlib.import_module("kindex.vectors")
    assert mod is not None

def test_is_available_is_callable():
    """Function is_available exists and is callable."""
    import importlib
    mod = importlib.import_module("kindex.vectors")
    fn = getattr(mod, "is_available", None)
    assert fn is not None, "'is_available' not found in kindex.vectors"
    assert callable(fn)

def test_embed_text_is_callable():
    """Function embed_text exists and is callable."""
    import importlib
    mod = importlib.import_module("kindex.vectors")
    fn = getattr(mod, "embed_text", None)
    assert fn is not None, "'embed_text' not found in kindex.vectors"
    assert callable(fn)

def test_ensure_vec_table_is_callable():
    """Function ensure_vec_table exists and is callable."""
    import importlib
    mod = importlib.import_module("kindex.vectors")
    fn = getattr(mod, "ensure_vec_table", None)
    assert fn is not None, "'ensure_vec_table' not found in kindex.vectors"
    assert callable(fn)

def test_upsert_embedding_is_callable():
    """Function upsert_embedding exists and is callable."""
    import importlib
    mod = importlib.import_module("kindex.vectors")
    fn = getattr(mod, "upsert_embedding", None)
    assert fn is not None, "'upsert_embedding' not found in kindex.vectors"
    assert callable(fn)

def test_vector_search_is_callable():
    """Function vector_search exists and is callable."""
    import importlib
    mod = importlib.import_module("kindex.vectors")
    fn = getattr(mod, "vector_search", None)
    assert fn is not None, "'vector_search' not found in kindex.vectors"
    assert callable(fn)

def test_index_all_nodes_is_callable():
    """Function index_all_nodes exists and is callable."""
    import importlib
    mod = importlib.import_module("kindex.vectors")
    fn = getattr(mod, "index_all_nodes", None)
    assert fn is not None, "'index_all_nodes' not found in kindex.vectors"
    assert callable(fn)
