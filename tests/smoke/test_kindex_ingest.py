"""Smoke tests for src/kindex/ingest.py (auto-generated by pact adopt)."""

import pytest

def test_import_kindex_ingest():
    """Module imports without error."""
    import importlib
    mod = importlib.import_module("kindex.ingest")
    assert mod is not None

def test_scan_projects_is_callable():
    """Function scan_projects exists and is callable."""
    import importlib
    mod = importlib.import_module("kindex.ingest")
    fn = getattr(mod, "scan_projects", None)
    assert fn is not None, "'scan_projects' not found in kindex.ingest"
    assert callable(fn)

def test_scan_sessions_is_callable():
    """Function scan_sessions exists and is callable."""
    import importlib
    mod = importlib.import_module("kindex.ingest")
    fn = getattr(mod, "scan_sessions", None)
    assert fn is not None, "'scan_sessions' not found in kindex.ingest"
    assert callable(fn)

def test_find_parent_kin_is_callable():
    """Function find_parent_kin exists and is callable."""
    import importlib
    mod = importlib.import_module("kindex.ingest")
    fn = getattr(mod, "find_parent_kin", None)
    assert fn is not None, "'find_parent_kin' not found in kindex.ingest"
    assert callable(fn)

def test_scan_kin_files_is_callable():
    """Function scan_kin_files exists and is callable."""
    import importlib
    mod = importlib.import_module("kindex.ingest")
    fn = getattr(mod, "scan_kin_files", None)
    assert fn is not None, "'scan_kin_files' not found in kindex.ingest"
    assert callable(fn)

def test_resolve_kin_chain_is_callable():
    """Function resolve_kin_chain exists and is callable."""
    import importlib
    mod = importlib.import_module("kindex.ingest")
    fn = getattr(mod, "resolve_kin_chain", None)
    assert fn is not None, "'resolve_kin_chain' not found in kindex.ingest"
    assert callable(fn)

def test_merge_kin_chain_is_callable():
    """Function merge_kin_chain exists and is callable."""
    import importlib
    mod = importlib.import_module("kindex.ingest")
    fn = getattr(mod, "merge_kin_chain", None)
    assert fn is not None, "'merge_kin_chain' not found in kindex.ingest"
    assert callable(fn)

def test_load_project_context_is_callable():
    """Function load_project_context exists and is callable."""
    import importlib
    mod = importlib.import_module("kindex.ingest")
    fn = getattr(mod, "load_project_context", None)
    assert fn is not None, "'load_project_context' not found in kindex.ingest"
    assert callable(fn)

def test_load_synonym_rings_is_callable():
    """Function load_synonym_rings exists and is callable."""
    import importlib
    mod = importlib.import_module("kindex.ingest")
    fn = getattr(mod, "load_synonym_rings", None)
    assert fn is not None, "'load_synonym_rings' not found in kindex.ingest"
    assert callable(fn)

def test_detect_expertise_is_callable():
    """Function detect_expertise exists and is callable."""
    import importlib
    mod = importlib.import_module("kindex.ingest")
    fn = getattr(mod, "detect_expertise", None)
    assert fn is not None, "'detect_expertise' not found in kindex.ingest"
    assert callable(fn)

def test_write_kin_index_is_callable():
    """Function write_kin_index exists and is callable."""
    import importlib
    mod = importlib.import_module("kindex.ingest")
    fn = getattr(mod, "write_kin_index", None)
    assert fn is not None, "'write_kin_index' not found in kindex.ingest"
    assert callable(fn)
